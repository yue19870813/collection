#!/usr/bin/python
#coding=utf-8
fruits = ['banana', 'apple',  'mango']
for index in range(len(fruits)):
	if index == 1:
		pass
	else:
		print "当前下标：", index
		print "当前水果：", fruits[index]


